# ─── .env.example (raíz) ──────────────────────────────────────────────────────
# Plantilla para docker-compose.prod.yml (despliegue en servidor propio).
# Copia como ".env" y reemplaza los valores.
#
# En desarrollo NO necesitas este archivo:
#   - Backend:  edita backend/.env
#   - Frontend: edita frontend/.env.local

# ─── Base de datos (producción) ───────────────────────────────────────────────
DB_USER="eduparche_user"
DB_PASSWORD="contraseña"
DB_NAME="eduparche_prod"

# ─── JWT ──────────────────────────────────────────────────────────────────────
# Genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="secreto-aleatorio-de-64-caracteres-minimo"

# ─── URLs públicas ────────────────────────────────────────────────────────────
FRONTEND_URL="https://tudominio.com"
NEXT_PUBLIC_API_URL="https://api.tudominio.com/api"
